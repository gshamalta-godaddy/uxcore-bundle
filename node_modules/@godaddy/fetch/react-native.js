const fetch = global.fetch;

fetch.Headers = global.Headers;
fetch.Request = global.Request;
fetch.Response = global.Response;
fetch.AbortController = global.AbortController;

module.exports = fetch;
