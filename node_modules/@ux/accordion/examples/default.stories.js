import Accordion from '@ux/accordion';
import '@ux/accordion/styles';
import Collapsible from '@ux/collapsible';
import '@ux/collapsible/styles';
import lipsum from 'bob-ross-lipsum';
import React from 'react';

export default function Default() {
  return (
    <Accordion title='An Accordion Example'>
      <Collapsible title={ lipsum(1) }>
        <ul>
          <li>something</li>
          <li>something</li>
          <li><button className='btn btn-primary'>testing sauce</button></li>
        </ul>
        <p>{ lipsum(10) }</p>
        <p>{ lipsum(10) }</p>
      </Collapsible>

      <Collapsible title={ lipsum(1) }>
        <p>{ lipsum(10) }</p>
      </Collapsible>

      <Collapsible title={ lipsum(1) }>
        <p>{ lipsum(10) }</p>
      </Collapsible>
    </Accordion>
  );
}
