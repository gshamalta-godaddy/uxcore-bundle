import { css } from 'linaria';
import intents from './intents.config.js';


export default css`
/* Accordion */
:global() {

  .ux-accordion {
    border-top: 1px solid ${intents.uxAccordion.borderColor};
    display: flex;
    align-items: stretch;
    justify-content: flex-start;
    flex-direction: column;

    .panel-collapse {
      &.collapse.in {
        overflow-y: visible;
      }
    }
  }
}
`;
