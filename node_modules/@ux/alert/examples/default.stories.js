import React from 'react';
import Alert from '../src';
import text from '@ux/text';

export default function Default() {
  const message = 'The quick brown dog jumps over the lazy fox';
  const title = <text.h3 as='title' text='You should do something' />;

  return <div className='spaced-group'>
    <Alert
      title={ title }
      id='success'
      emphasis='success'>
      { message }
    </Alert>

    <Alert
      title={ title }
      id='warning'
      emphasis='warning'>
      { message }
    </Alert>

    <Alert
      title={ title }
      id='critical'
      emphasis='critical'>
      { message }
    </Alert>

    <Alert
      title={ title }
      id='info'
      emphasis='info'>
      { message }
    </Alert>

    <Alert
      title={ title }
      id='highlight'
      emphasis='highlight'>
      { message }
    </Alert>

    <Alert
      title={ title }
      id='neutral'
      emphasis='neutral'>
      { message }
    </Alert>

    <Alert
      title={ title }
      id='internal'
      emphasis='internal'>
      { message }
    </Alert>
  </div>;
}
