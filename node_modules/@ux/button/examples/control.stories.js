import Button from '../src';
import React from 'react';
import Star from '@ux/icon/star';

export default function ControlButton() {
  return (
    <div className='spaced-group'>
      <Button design='control' text='Control Button'/>

      <Button design='control' text='Control Button Disabled' disabled={ true }/>

      <Button design='control' text='Control Button with icon' icon={ <Star/> } className='test-with-icon'/>

      <Button design='control' text='Small Control Button' size='small'/>
    </div>
  );
}
