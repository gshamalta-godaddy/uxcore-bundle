import Button from '../src';
import React from 'react';
import Star from '@ux/icon/star';

export default function StatelessButton() {
  return (
    <div className='spaced-group'>
      <Button design='stateless' text='Stateless Button'/>

      <Button design='stateless' text='Stateless Button as link' href='#'/>

      <Button design='stateless' text='Small Stateless Button' size='small'/>

      <Button design='stateless' icon={ <Star/> } className='test-with-icon'/>
    </div>
  );
}
