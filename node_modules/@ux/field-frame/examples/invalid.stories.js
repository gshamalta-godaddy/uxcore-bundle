import FieldFrame from '../src';
import Button from '@ux/button';
import React from 'react';

export default function InvalidExample() {
  const isInvalid = true;

  return (
    <FieldFrame invalid={ isInvalid }>
      <Button text='With a button as invalid' design='critical'/>
    </FieldFrame>
  );
}
