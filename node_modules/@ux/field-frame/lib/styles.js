"use strict";

var _interopRequireDefault = require("@babel/runtime/helpers/interopRequireDefault");

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = void 0;

var _linaria = require("linaria");

var _intentsConfig = _interopRequireDefault(require("./intents.config.js"));

function flyout(placement) {
  // Placement can be defined as location{-start|end}
  const [location] = placement.split('-');
  return `
    &[data-flyout*="${location}"] {
      border-${location}-left-radius: 0;
      border-${location}-right-radius: 0;
    }

    &[data-flyout] > .ux-flyout[data-placement*="${location}"] {
      border-radius: 0;
      border-${location}-left-radius: ${_intentsConfig.default.uxFieldFrame.borderRadius};
      border-${location}-right-radius: ${_intentsConfig.default.uxFieldFrame.borderRadius};
      border-width: 0;
      border-left-width: inherit;
      border-right-width: inherit;
      border-${location}: inherit;
      overflow-x: hidden;
      overflow-y: auto;
    }
  `;
}

var _default = "s2h4jrs";
exports.default = _default;