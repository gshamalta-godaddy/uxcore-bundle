import { FormElement, Form } from '@ux/uxcore2';
import React from 'react';

export default class Autocomplete extends React.Component {
  render() {
    return (
      <Form action='#'>
        <FormElement label='Autocomplete Email' name='example' autoComplete='email' />
      </Form>
    );
  }
}
