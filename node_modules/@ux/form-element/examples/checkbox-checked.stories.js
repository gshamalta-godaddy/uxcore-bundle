import { Checkbox, Form } from '@ux/uxcore2';
import React from 'react';

export default class CheckboxChecked extends React.Component {
  render() {
    return (
      <Form>
        <Checkbox label='I am already checked' value='foo' name='bar2' defaultChecked />
      </Form>
    );
  }
}
