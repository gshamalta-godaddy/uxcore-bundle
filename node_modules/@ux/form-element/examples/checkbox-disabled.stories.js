import { Checkbox, Form } from '@ux/uxcore2';
import React from 'react';

export default class CheckboxDisabled extends React.Component {
  render() {
    return (
      <Form>
        <Checkbox disabled label="Can't touch this." value='hammer' name='time' />
        <Checkbox disabled label="Can't uncheck this." value='checked' name='yes' defaultChecked />
      </Form>
    );
  }
}

