import React from 'react';
import { Checkbox, Form } from '@ux/uxcore2';

export default class CheckboxInline extends React.Component {
  render() {
    return (
      <Form inline >
        <Checkbox label='foo' inline />
        <Checkbox label='bar' inline />
        <Checkbox label='baaz' inline />
        <Checkbox label='quux' inline />
      </Form>
    );
  }
}
