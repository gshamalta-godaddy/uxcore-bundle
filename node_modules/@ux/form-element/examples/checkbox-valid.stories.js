import { Checkbox, Form } from '@ux/uxcore2';
import React from 'react';

export default class CheckboxValid extends React.Component {
  render() {
    return (
      <Form>
        <Checkbox label='Valid' value='foo' name='bar' validates={ true } defaultChecked />
      </Form>
    );
  }
}
