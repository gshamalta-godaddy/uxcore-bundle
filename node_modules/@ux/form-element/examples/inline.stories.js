import { FormElement, Form } from '@ux/uxcore2';
import React from 'react';

export default class Inline extends React.Component {
  render() {
    return (
      <Form inline>
        <FormElement label='one' defaultValue='inline input' name='example' />
        <FormElement label='two' defaultValue='inline input' name='example' />
        <FormElement label='three' defaultValue='inline input' name='example' />
      </Form>
    );
  }
}
