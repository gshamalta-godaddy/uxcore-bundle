import { Form, Password } from '@ux/uxcore2';
import React from 'react';

import Show from '@ux/icon/show';
import Hide from '@ux/icon/hide';

export default class PasswordIcons extends React.Component {
  render() {
    return (
      <Form>
        <Password id='icons' label='Password' show={ <Show /> } hide={ <Hide /> } />
      </Form>
    );
  }
}
