import { Form, Password } from '@ux/uxcore2';
import React from 'react';

import Show from '@ux/icon/show';
import Hide from '@ux/icon/hide';

export default class PasswordNavigation extends React.Component {
  render() {
    return (
      <Form>
        <Password
          id='nav'
          label='Password'
          href='/forgot'
          navigation={ 'Forgot?' }
          show={ <Show /> }
          hide={ <Hide /> } />
      </Form>
    );
  }
}
