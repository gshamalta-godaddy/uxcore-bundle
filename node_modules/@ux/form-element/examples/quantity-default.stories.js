import { QuantitySelector } from '@ux/uxcore2';
import React from 'react';

export default class QuantityDefault extends React.Component {
  render() {
    return (
      <div>
        <QuantitySelector
          id='default-quantity-selector'
          label={ <div className='text-uppercase'>Select a value between 2 and 15</div> }
          min={ 2 }
          max={ 15 }
          defaultValue={ 5 }
          errorMessage='Your input is invalid.' />
      </div>
    );
  }
}
