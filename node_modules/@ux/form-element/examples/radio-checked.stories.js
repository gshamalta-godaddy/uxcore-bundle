import { Radio, Form } from '@ux/uxcore2';
import React from 'react';

export default class RadioCheckedExample extends React.Component {
  render() {
    return (
      <Form>
        <Radio label='I am already checked' value='foo' name='bar' defaultChecked />
      </Form>
    );
  }
}
