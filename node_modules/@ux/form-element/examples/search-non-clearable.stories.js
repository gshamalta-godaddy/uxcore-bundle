import { Search } from '@ux/uxcore2';
import React from 'react';

export default class SearchNonClearable extends React.Component {
  render() {
    const data = [
      'Apples',
      'Bananas',
      'Blueberries',
      'Grapes',
      'Peaches',
      'Snozberries',
      'Yams'
    ];

    return (
      <Search id='non-clearable' data={ data } placeholder='This search field is not clearable' clearable={ false } />
    );
  }
}
