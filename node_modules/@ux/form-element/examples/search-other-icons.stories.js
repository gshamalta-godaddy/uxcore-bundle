import { Search } from '@ux/uxcore2';
import React from 'react';
import Show from '@ux/icon/show';
import Trash from '@ux/icon/trash';
import Star from '@ux/icon/star';

export default class SearchOtherIcons extends React.Component {
  render() {
    return (
      <React.Fragment>
        <Search icon={ Show } id='smallShow' size='small' placeholder='Small' />
        <Search icon={ Trash } id='regularTrash' placeholder='Default' />
        <Search icon={ Star } id='regularStar' placeholder='Default with star' />
      </React.Fragment>
    );
  }
}
