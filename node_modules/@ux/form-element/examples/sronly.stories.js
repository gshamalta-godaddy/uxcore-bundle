import React from 'react';
import { FormElement, Form } from '@ux/uxcore2';

export default class SrOnly extends React.Component {
  render() {
    return (
      <Form>
        <FormElement hideLabel label='This label is being announced by the screen reader.' />
      </Form>
    );
  }
}
