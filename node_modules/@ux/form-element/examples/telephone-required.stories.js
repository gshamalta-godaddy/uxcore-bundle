import { Form, TelephoneInput } from '@ux/uxcore2';
import data from './countries.json';
import React from 'react';

export default class TelephoneRequired extends React.Component {
  render() {
    return (
      <Form>
        <TelephoneInput
          countryData={ data }
          countryKey={ 'US' }
          marketId={ 'en-US' }
          name={ 'phone' }
          id={ 'phone-field' }
          label={ 'Required Value' }
          required={ 'this is required' }
          onChangeHandler={ () => {} }
        />
      </Form>
    );
  }
}
