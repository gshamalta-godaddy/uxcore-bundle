import { FormElement, Form } from '@ux/uxcore2';
import React from 'react';

export default class DifferentTypes extends React.Component {
  render() {
    return (
      <Form action='#'>
        <FormElement label='Username' type='text' name='example' />
        <FormElement label='Search' type='search' name='searching' />
        <FormElement label='Email address' type='email' name='email' />
        <FormElement label='Numeric' type='number' name='numeric' />
      </Form>
    );
  }
}
