/* global cy */
describe('@ux/icon SSR', function () {
  it('hydrates the icon without any issues', function () {
    cy.visit('http://localhost:9921');

    cy.get('svg use[*|href$="cloud-upload"]').should('have.attr', 'fill');
  });
});
