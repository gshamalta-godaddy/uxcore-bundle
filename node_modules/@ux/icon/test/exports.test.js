
const { name } = require('../package.json');
const { describe, it } = require('mocha');

describe('Package exports', function () {
  it('does not throw when CSS is required in a Node.js process', function () {
    require(name + '/styles');
  });

  it('has an empty file as fallback', function () {
    require('../empty.js');
  });
});
