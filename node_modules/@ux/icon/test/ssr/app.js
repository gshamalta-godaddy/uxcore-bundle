const CloudUpload = require('../../cloud-upload');
const React = require('react');

/**
 * Simple application that demonstrates that SSR and hydration works as
 * intended.
 *
 * @returns {React.Component} Our application shell.
 * @private
 */
function App() {
  return React.createElement('div', { id: 'my-test-app' },
    React.createElement('p', null, 'This should display the icon in red'),
    React.createElement(CloudUpload, { width: 100, height: 100, fill: 'red' })
  );
}

module.exports = App;
