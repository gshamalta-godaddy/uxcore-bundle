import React from 'react';
import Listbox from '../src';

const ITEMS = [
  {
    id: 'add',
    children: 'Add Item'
  },
  {
    id: 'edit',
    children: 'Edit User'
  },
  {
    id: 'delete',
    children: 'Delete a thing'
  }
];

export default function Default(args) {
  return (
    <Listbox items={ ITEMS } { ...args }/>
  );
}
