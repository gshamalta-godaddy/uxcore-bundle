import { css } from 'linaria';
import Intents from '@ux/intents';

const intents = new Intents();

import '@ux/disrupt/src/styles';

export default css`
  :global() {
    .ux-message-overlay-content {
      padding: calc(${intents.ux.box.density} * 4);
      background-color: ${intents.ux.box.backgroundColor};
      color: ${intents.ux.box.foregroundColor};
      border: ${intents.ux.box.borderWidth} solid ${intents.ux.box.borderColor};
      border-radius: ${intents.ux.box.borderRadius};

      font-family: ${intents.ux.text.fontFamily};
      font-size: ${intents.ux.text.fontSize};
      font-weight: ${intents.ux.text.fontWeight};
      line-height: ${intents.ux.text.lineHeight};
    }
  }
`;
