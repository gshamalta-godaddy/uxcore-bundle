[
  {
    "description": "Pivots class to render multiple pivot components\n\n@displayName Pivots\n@function Pivots\n@param {ReactProps} props - component props\n@returns {ReactElement} - React Element\n@public",
    "displayName": "Pivots",
    "methods": [],
    "props": {
      "design": {
        "type": {
          "name": "string"
        },
        "required": false,
        "description": "default is tile buttons meant for tablet/desktop.\n`slats` which is the default mobile rendering can be forced for tablet/desktop by passing `slats` as design"
      },
      "grid": {
        "type": {
          "name": "object"
        },
        "required": false,
        "description": "the bootstrap grid specification",
        "defaultValue": {
          "value": "{\n  md: 6,\n  xl: 4\n}",
          "computed": false
        }
      },
      "pivotList": {
        "type": {
          "name": "array"
        },
        "required": false,
        "description": "Array of pivot data structures to generate pivot components"
      },
      "xsGraphics": {
        "type": {
          "name": "bool"
        },
        "required": false,
        "description": "force graphics to render on mobile"
      },
      "xsSubtitles": {
        "type": {
          "name": "bool"
        },
        "required": false,
        "description": "force subtitles to render on mobile"
      },
      "selectedIndex": {
        "type": {
          "name": "custom",
          "raw": "(props) => {\n  const { selectedIndex, pivotList } = props;\n  if (!(selectedIndex && pivotList)) return null;\n  const error = (\n    typeof selectedIndex !== 'number'\n    && 'selectedIndex must be a number'\n  ) || (\n    (selectedIndex > (pivotList.length - 1) || selectedIndex < -1)\n    && 'selectedIndex is invalid'\n  );\n  return error ? new Error(error) : null;\n}"
        },
        "required": false,
        "description": "Default selected tile/slat when initially rendered\n@param {Object} props React props\n@returns {Error|null} validation",
        "defaultValue": {
          "value": "-1",
          "computed": false
        }
      }
    }
  }
]