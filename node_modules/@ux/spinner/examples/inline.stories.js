import Spinner from '../src';
import React from 'react';

export default function Inline() {
  const styles = {
    display: 'flex'
  };

  return (
    <div style={ styles }>
      This is an inline  <Spinner size='sm' />  spinner.
    </div>
  );
}
