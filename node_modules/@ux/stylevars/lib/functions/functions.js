"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.themeColor = themeColor;
exports.themeColorLevel = themeColorLevel;

var _polished = require("polished");

function themeColor() {
  let key = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : 'primary';
  let themeColors = arguments.length > 1 ? arguments[1] : undefined;
  return themeColors[key];
}

function themeColorLevel() {
  let name = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : 'primary';
  let level = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : 0;
  let themeColors = arguments.length > 2 ? arguments[2] : undefined;
  let themeColorInterval = arguments.length > 3 && arguments[3] !== undefined ? arguments[3] : 0.08;
  const color = themeColor(name, themeColors);
  const colorBase = level > 0 ? '#000' : '#fff';
  level = Math.abs(level);
  return (0, _polished.mix)(level * themeColorInterval, color, colorBase);
}