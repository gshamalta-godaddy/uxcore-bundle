"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.marketFont = marketFont;
exports.marketGroup = marketGroup;

var _markets = require("../variables/_markets");

var _uxvars = require("../variables/uxvars");

const marketingFontKeys = ['marketing', 'sage', 'sage-bold'];
const hasOwnProperty = Object.prototype.hasOwnProperty;

function marketGroup() {
  const group = _markets.marketGroups[_uxvars.uxcoreMarket];
  if (typeof group === 'string') return group;

  if (typeof group === 'object' && hasOwnProperty.call(group, _uxvars.uxcoreRegion)) {
    return group[_uxvars.uxcoreRegion];
  }

  return 'brand';
}

function marketFont() {
  let font = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : 'base';
  let property = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : 'family';
  const mktGroup = marketGroup();
  const weights = _markets.marketFonts[mktGroup];
  let fontMap = weights[font];

  if (typeof fontMap === 'undefined') {
    if (marketingFontKeys.includes(font)) {
      fontMap = weights['primary-bold'];
    } else {
      fontMap = weights.base;
    }
  }

  return fontMap[property];
}