"use strict";

var _interopRequireDefault = require("@babel/runtime/helpers/interopRequireDefault");

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = palette;

var _colors = _interopRequireDefault(require("../variables/_colors"));

function palette(paletteName) {
  let tone = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : 'base';
  let color = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : 'color';

  if (paletteName === 'grey') {
    paletteName = 'gray';
  }

  if (_colors.default[paletteName] === 'undefined') {
    throw new Error(`${paletteName} is not a palette color.`);
  }

  if (_colors.default[paletteName][tone] === 'undefined') {
    throw new Error(`${paletteName} is not a palette tone.`);
  }

  return _colors.default[paletteName][tone][color];
}