"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = bgVariant;

var _hover = require("./hover");

// Contextual backgrounds
function bgVariant(parent, color) {
  let bg = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : 'transparent';
  return `
    ${parent} {
      color: ${color};
      background-color: ${bg};
    }

    a${parent} {
      ${(0, _hover.hoverFocus)(`background-color: ${color} !important;`)}
      filter: brightness(90%);
    }
  `;
}