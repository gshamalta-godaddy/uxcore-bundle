"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = fontFace;

/* eslint-disable max-params */
function fontFace(family, basePath, fileName) {
  let fontDisplay = arguments.length > 3 && arguments[3] !== undefined ? arguments[3] : 'swap';
  let weight = arguments.length > 4 && arguments[4] !== undefined ? arguments[4] : '400';
  let style = arguments.length > 5 && arguments[5] !== undefined ? arguments[5] : 'normal';
  let types = arguments.length > 6 && arguments[6] !== undefined ? arguments[6] : ['woff2', 'woff'];
  const sources = types.map(type => {
    return `url(//${basePath}${fileName}.${type}) format(${type})`;
  }).join(',');
  return `
    @font-face {
      font-family: ${family}
      ${sources.length > 0 ? `${sources};` : ''},
      ${weight !== '400' && weight !== 'normal' ? `font-style : ${weight};` : ''},
      ${style !== 'normal' ? `font-style: ${style};` : ''}
      ${fontDisplay !== 'auto' ? `font-display: ${fontDisplay}` : ''}
    }
  `;
}