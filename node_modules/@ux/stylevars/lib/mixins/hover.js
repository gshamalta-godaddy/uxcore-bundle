"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.hover = hover;
exports.hoverFocus = hoverFocus;
exports.hoverFocusActive = hoverFocusActive;
exports.plainHoverFocus = plainHoverFocus;

var _variables = require("../variables/_variables");

function hover(content) {
  return `
    &:hover {
      ${content}
    }
  `;
}

function hoverFocus(content) {
  if (_variables.enableHoverMediaQuery) {
    return `
      &:focus {
        ${content};
      }
      ${hover(content)}
    `;
  }

  return `
    &:focus,
    &:hover {
      ${content};
    }
  `;
}

function plainHoverFocus(content) {
  if (_variables.enableHoverMediaQuery) {
    return `
      &,
      &:focus {
        ${content}
      }
      ${hover(content)}
    `;
  }

  return `
    &,
    &:focus,
    &:hover {
      ${content}
    }
  `;
}

function hoverFocusActive(content) {
  if (_variables.enableHoverMediaQuery) {
    return `
      &:focus,
      &:active {
        ${content}
      }
      ${hover(content)}
    `;
  }

  return `
  &:focus,
  &:active,
  &:hover {
    ${content}
  }
  `;
}