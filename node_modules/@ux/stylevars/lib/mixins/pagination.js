"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.paginationActive = paginationActive;
exports.paginationSize = paginationSize;

// Pagination
function paginationSize(fontSize, paginationHeightWidth) {
  return `
    .page-link {
      padding: 0 1px;
      font-size: ${fontSize};
      min-width: ${paginationHeightWidth};
      min-height: ${paginationHeightWidth};
      line-height: ${paginationHeightWidth};
    }
  `;
}

function paginationActive(paginationHeightWidth) {
  return `
    min-height: ${paginationHeightWidth};
    line-height: ${paginationHeightWidth};
    height: ${paginationHeightWidth};
  `;
}