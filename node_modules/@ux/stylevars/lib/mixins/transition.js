"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = transition;

var _variables = require("../variables/_variables");

function transition(transitions) {
  if (_variables.enableTransitions) {
    if (transitions.length === 0) {
      return `transition: ${_variables.transitionBase};`;
    }

    return `transition: ${transitions};`;
  }
}