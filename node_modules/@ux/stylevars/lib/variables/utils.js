"use strict";

var _interopRequireDefault = require("@babel/runtime/helpers/interopRequireDefault");

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.keysToCamelCase = keysToCamelCase;

var _camelcase = _interopRequireDefault(require("camelcase"));

/**
 * Convert variables from dash-case (scss-style) to camelCase (js-style)
 *
 * @param {object} variables - Override variables
 * @returns {object} vars
 */
function keysToCamelCase(variables) {
  return Object.entries(variables || {}).reduce((acc, _ref) => {
    let [key, value] = _ref;
    acc[(0, _camelcase.default)(key)] = value;
    return acc;
  }, {});
}