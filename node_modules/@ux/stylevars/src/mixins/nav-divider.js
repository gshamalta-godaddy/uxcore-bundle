import { math } from 'polished';
import { spacer } from '../variables/_variables';

export default function navDivider(color = '#e5e5e5') {
  return `
    height: 0;
    margin: ${math(`${spacer} / 2`)} 0;
    overflow: hidden;
    border-top: 1px solid ${color};
  `;
}
