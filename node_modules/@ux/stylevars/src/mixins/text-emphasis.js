import { hoverFocus } from './hover';

export default function textEmphasisVariant(parent, color) {
  return `
    ${parent} {
      color: ${color} !important;
    }
    a${parent} {  
      ${hoverFocus(`color: ${color} !important;`)}
    }
  `;
}
