import { enableTransitions, transitionBase } from '../variables/_variables';

export default function transition(transitions) {
  if (enableTransitions) {
    if (transitions.length === 0) {
      return `transition: ${transitionBase};`;
    }
    return `transition: ${transitions};`;
  }
}
