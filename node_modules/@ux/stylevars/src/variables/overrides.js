// this can be overridden by webpack alias for different palettes
import { colors, fonts, variables } from '../../overrides/palette-overrides.json';
import { keysToCamelCase } from './utils';

const fixedVars = keysToCamelCase(variables);

export {
  colors,
  fonts,
  fixedVars as variables
};
