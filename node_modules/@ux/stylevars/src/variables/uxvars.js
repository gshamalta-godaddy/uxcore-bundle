/* eslint-disable no-process-env */

import * as overrides from '../../overrides/env-overrides.json';

const {
  palettePath = process.env.UXCORE_PALETTE || process.env.PALETTE
} = overrides;

const {
  uxcoreEnv = process.env.NODE_ENV,
  uxcoreMarket = (process.env.UXCORE_LANG || 'en').toLowerCase(),
  uxcoreRegion = (process.env.UXCORE_REGION || 'US').toLowerCase(),
  uxcoreLabel = (process.env.UXCORE_LABEL || 'gd').toLowerCase(),
  uxcorePalette = palettePath,
  uxcorePaletteBuild = !!palettePath
} = overrides;

export {
  uxcoreEnv,
  uxcoreMarket,
  uxcoreRegion,
  uxcoreLabel,
  uxcorePalette,
  uxcorePaletteBuild
};
