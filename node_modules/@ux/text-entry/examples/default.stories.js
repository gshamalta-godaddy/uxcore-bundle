import TextEntry from '../src';
import React from 'react';

export default function Default() {
  return (
    <fieldset>
      <TextEntry
        type='text'
        id='default'/>
    </fieldset>
  );
}
