import React, { useState, useCallback } from 'react';
import Toggle from '@ux/toggle';
import '@ux/toggle/styles';

export default function UniqueIdToggle() {
  const [state, setState] = useState({
    checked1: true,
    checked2: true,
    checked3: true
  });

  const onChange = useCallback((name) => {
    setState({ ...state, [name]: !state[name] });
  });

  const toggleLabel1 = state.checked1 ? 'on' : 'off';
  const toggleLabel2 = state.checked2 ? 'on' : 'off';
  const toggleLabel3 = state.checked3 ? 'on' : 'off';

  return (
    <p>
      <span>I am </span>
      <Toggle onChange={ () => onChange('checked1') } id='id-checked1' checked={ state.checked1 } label={ toggleLabel1 } />
      <Toggle onChange={ () => onChange('checked2') } id='id-checked2' checked={ state.checked2 } label={ toggleLabel2 } />
      <Toggle onChange={ () => onChange('checked3') } id='id-checked3' checked={ state.checked3 } label={ toggleLabel3 } />
      <span> excited!</span>
    </p>
  );
}
