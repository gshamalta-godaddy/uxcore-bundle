import React from 'react';
import Tooltip from '../src';

export default function Closable(args) {
  return (
    <Tooltip { ...args }>
      Closable tooltip
    </Tooltip>
  );
}

Closable.args = {
  id: 'closable-tooltip',
  message: 'Click on the cross to close the tooltip',
  close: true
};
