import React from 'react';
import Tooltip from '../src';
import Wand from '@ux/icon/wand';
import lipsum from 'bob-ross-lipsum';

export default function SwapIcon(args) {
  return (
    <div>
      Render with a bit of <Tooltip
        text={ <span className='text-default'><Wand width={ 18 } height={ 18 } /></span> }
        message={ lipsum(3) }
        { ...args }
      ></Tooltip>
    </div>
  );
}

SwapIcon.args = {
  id: 'swap-icon'
};
