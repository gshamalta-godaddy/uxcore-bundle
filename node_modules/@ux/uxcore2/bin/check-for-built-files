#!/usr/bin/env bash

PRESENT=$(ls -1 dist/**/uxcore2.min.* | wc -l | xargs)

echo "PRESENT = $PRESENT"

if [[ ${PRESENT} -ge 4 ]]; 
then
    echo "Minified files are already built"
else
    npm run build
fi
